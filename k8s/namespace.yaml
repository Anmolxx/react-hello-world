apiVersion: v1
kind: Namespace
metadata:
  name: react-app
  labels:
    name: react-app
    environment: production
  annotations:
    description: "Namespace for React application and related services"

